# PENDIENTES PROTOTIPO — C6 SKIP13 root-fix source-gate HOLD

Pendiente: corregir exclusivamente el harness source-only para que `--self-test` no active lógica module-level del módulo importado. Mantener namespaces v2 y cero provider hasta PASS estático. Después requerirá autorización distinta para una única revalidación SKIP13 read-only.

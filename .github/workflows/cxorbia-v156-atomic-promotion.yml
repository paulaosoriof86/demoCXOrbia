name: CXOrbia V156 Direct Empalme — HISTORICAL INERT

# RC15 emergency containment.
# Authorized by Paula in the current conversation on 2026-08-21.
# Historical V156 transport/apply authority is intentionally inert.
# This workflow must never retrieve, apply, commit, push, deploy, access provider credentials, or mutate product state.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-inert:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Historical lane is inert
        run: echo 'RC15_INERT_HISTORICAL_V156_LANE'

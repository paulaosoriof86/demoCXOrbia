name: CXOrbia C6 Shopper Equivalent Universe Source Only — Frozen

on:
  push:
    branches:
      - docs-tya-v6-v71-audit
    paths:
      - backend/config/corte6-shopper-equivalent-universe-source-only-request-retired.json

permissions:
  contents: read
  statuses: write

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Consumed source-only integration trigger frozen; a new explicit authorization is required.'

name: RETIRED CXOrbia C6 Auth Update-Universe Batch Revalidation V3

on:
  push:
    branches: [__cxorbia_retired_workflow_never_run__]

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo retired

name: CXOrbia RC Phase A Staging Deploy

# Controlled Hosting preview only. No Firestore/Storage rules, Functions, imports or providers.
on:
  workflow_dispatch:
  push:
    branches:
      - docs-tya-v6-v71-audit
    paths:
      - '.github/workflows/cxorbia-rc-phase-a-staging-deploy.yml'

permissions:
  contents: read
  pull-requests: write
  checks: read

defaults:
  run:
    shell: bash

jobs:
  staging-deploy:
    name: RC Phase A staging deploy hosting only
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Predeploy gate
        run: |
          node tools/release/tya-rc-phase-a-predeploy-gate.mjs --out .tmp/rc-phase-a-staging-predeploy

      - name: Drift gate
        run: |
          node tools/release/tya-rc-phase-a-drift-gate.mjs \
            --validated d6abab620589abf9be26e0a3884f92c4b342fb9b \
            --out .tmp/rc-phase-a-staging-drift

      - name: Secret availability check
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CXORBIA_BACKEND_DEV }}
        run: |
          if [ -z "$FIREBASE_SERVICE_ACCOUNT_JSON" ]; then
            echo "Missing FIREBASE_SERVICE_ACCOUNT_CXORBIA_BACKEND_DEV secret."
            echo "Deployment was intentionally blocked before any Firebase call."
            exit 1
          fi
          echo "Firebase service account secret is present."

      - name: Deploy Hosting preview channel
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CXORBIA_BACKEND_DEV }}
          projectId: cxorbia-backend-dev
          target: cxorbia-dev
          channelId: rc-phase-a
          expires: 7d

      - name: Upload staging deploy reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rc-phase-a-staging-deploy-report
          path: .tmp
          if-no-files-found: ignore
          retention-days: 14

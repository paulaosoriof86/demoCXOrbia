name: CXOrbia Corte 6 Protected Runtime Hosting DEV

on:
  push:
    branches:
      - docs-tya-v6-v71-audit
    paths:
      - 'backend/config/corte6-protected-session-continuity-hosting-execute.json'

permissions:
  contents: write
  statuses: write

concurrency:
  group: cxorbia-corte6-protected-runtime-hosting
  cancel-in-progress: false

jobs:
  hosting:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Validate exact protected DEV authorization + execute marker
        id: gate
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs=require('fs');
          const rp='backend/config/corte6-credential-continuity-hosting-request.json';
          const xp='backend/config/corte6-protected-session-continuity-hosting-execute.json';
          const r=JSON.parse(fs.readFileSync(rp,'utf8'));
          const x=JSON.parse(fs.readFileSync(xp,'utf8'));
          if(r.schemaVersion!=='cxorbia.corte6.credential-continuity-hosting-request.v1') throw new Error('request_schema_mismatch');
          if(x.schemaVersion!=='cxorbia.corte6.protected-session-continuity-hosting-execute.v1') throw new Error('execute_schema_mismatch');
          for(const o of [r,x]) if(o.repository!=='paulaosoriof86/demoCXOrbia'||o.branch!=='docs-tya-v6-v71-audit'||o.firebaseProjectId!=='cxorbia-backend-dev'||o.hostingSite!=='cxorbia-backend-dev'||o.hostingTarget!=='cxorbia-dev'||o.tenantId!=='tya'||o.projectId!=='cinepolis') throw new Error('identity_mismatch');
          if(r.singleHostingDeployMax!==1||r.hostingDeployExecutions!==0||r.createFirebaseProject!==false||r.createHostingSite!==false) throw new Error('one_shot_target_mismatch');
          if(r.enabled!==true||r.status!=='authorized_waiting_execute'||r.consumed!==false||r.authorizedBy!=='Paula'||!r.authorizationText||!r.authorizationId) throw new Error('authorization_missing');
          if(r.scope!=='single_existing_dev_hosting_redeploy_for_protected_runtime_human_visual'||r.protectedRuntimeRequired!==true||r.sessionContinuityFixRequired!==true) throw new Error('protected_scope_mismatch');
          if(x.enabled!==true||x.status!=='execute_authorized_once'||x.consumed!==false||x.requestId!==r.requestId||x.authorizationId!==r.authorizationId) throw new Error('execute_marker_not_authorized');
          for(const k of ['authWrites','firestoreWrites','rulesDeploys','storageWrites','hrWrites','legacyWrites','paymentsWrites','functionsDeploys','makeWrites','geminiCalls','cloudRunDeploys']) if(Number(r[k]||0)!==0||Number(x[k]||0)!==0) throw new Error('unsafe:'+k);
          if(r.production!==false||r.merge!==false||x.production!==false||x.merge!==false) throw new Error('unsafe_scope');
          console.log(JSON.stringify({decision:'AUTHORIZED_PROTECTED_SESSION_CONTINUITY_HOSTING_READY',authorizationId:r.authorizationId}));
          NODE

      - name: Static protected runtime + session continuity contract
        run: |
          set -euo pipefail
          node --check app/core/backend-browser-auth.js
          node --check app/core/backend-config-preview-dev.js
          node --check app/core/backend-protected-dev-mode.js
          node --check app/core/backend-protected-dev-session-continuity.js
          node --check app/core/backend-firebase.js
          node --check app/adapters/tya-live-source-refresh-watch.js
          node --check tools/release/cxorbia-corte6-credential-continuity-hosting-prepare.mjs
          node --check tools/release/cxorbia-existing-hosting-dev-direct-deploy.mjs
          grep -q "YES_PAULA_20260730_PROTECTED_DEV" app/core/backend-protected-dev-mode.js
          grep -q "humanVisualSourceSafe:false" app/core/backend-protected-dev-mode.js
          grep -q "readOnly:true" app/core/backend-protected-dev-mode.js
          grep -q "writeMode:'disabled'" app/core/backend-protected-dev-mode.js
          grep -q "persist:'local'" app/core/backend-protected-dev-mode.js
          grep -q "reuseAuthenticatedSession:true" app/core/backend-protected-dev-mode.js
          grep -q "backend-protected-dev-session-continuity.js" app/index-backend-dev.html
          grep -q "Persistence.LOCAL" app/core/backend-protected-dev-session-continuity.js
          grep -q "Persistence.SESSION" app/core/backend-protected-dev-session-continuity.js
          grep -q "credentialsEmbedded:false" app/core/backend-protected-dev-session-continuity.js
          grep -q "claimsBypass:false" app/core/backend-protected-dev-session-continuity.js
          grep -q "rulesBypass:false" app/core/backend-protected-dev-session-continuity.js
          grep -q "SHOPPER_SCOPE_REQUIRED" app/core/backend-browser-auth.js
          grep -q "ctx.shopperId" app/core/backend-browser-auth.js
          grep -q "loadAuthorizedShoppers" app/core/backend-firebase.js
          grep -q "visitsForShopper" app/core/backend-protected-dev-mode.js
          grep -q "submitida" app/core/backend-protected-dev-mode.js
          grep -q "humanVisualSourceSafe: true" app/core/backend-config-preview-dev.js
          grep -q "enabled: false" app/core/backend-config-preview-dev.js

      - name: Load canonical DEV service account
        env:
          EXISTING_DEV_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CXORBIA_BACKEND_DEV }}
        run: |
          set -euo pipefail
          mkdir -p .tmp/c6-credential-continuity-hosting
          node <<'NODE'
          const fs=require('fs');const raw=process.env.EXISTING_DEV_JSON||'';let p=null;try{p=JSON.parse(raw);}catch{}
          if(!(p&&p.type==='service_account'&&p.project_id==='cxorbia-backend-dev'&&p.private_key))throw new Error('credential_invalid');
          fs.writeFileSync('.tmp/c6-credential-continuity-hosting/credentials.json',raw,{mode:0o600});
          NODE
          echo "GOOGLE_APPLICATION_CREDENTIALS=${GITHUB_WORKSPACE}/.tmp/c6-credential-continuity-hosting/credentials.json" >> "$GITHUB_ENV"

      - name: Build exact existing Hosting DEV package
        env:
          CXORBIA_EXPECTED_PROJECT: cxorbia-backend-dev
          CXORBIA_EXPECTED_SITE: cxorbia-backend-dev
          CXORBIA_HOSTING_REQUEST: backend/config/corte6-credential-continuity-hosting-request.json
          CXORBIA_IMPORT_EVIDENCE: app/docs/evidence/CORTE6-CREDENTIAL-IMPORT-LATEST.json
          CXORBIA_HOSTING_OUT_DIR: .tmp/corte6-credential-continuity-hosting
        run: |
          set -euo pipefail
          npm install --no-save --ignore-scripts firebase-admin@13.4.0 >/dev/null 2>&1
          node tools/release/cxorbia-corte6-credential-continuity-hosting-prepare.mjs
          node <<'NODE'
          const fs=require('fs');
          const r=JSON.parse(fs.readFileSync('backend/config/corte6-credential-continuity-hosting-request.json','utf8'));
          const proof={schemaVersion:'cxorbia.corte6-protected-runtime-hosting-proof.v2',generatedAt:new Date().toISOString(),authorizationId:r.authorizationId,environment:'DEV',firebaseProjectId:'cxorbia-backend-dev',hostingSite:'cxorbia-backend-dev',hostingTarget:'cxorbia-dev',tenantId:'tya',projectId:'cinepolis',entrypoint:r.canonicalEntrypoint,protectedRuntime:true,authMode:'firebase-auth-custom-claims-rbac',sessionContinuity:'firebase-local-after-first-real-auth',readOnly:true,writeMode:'disabled',firestoreWrites:0,authWrites:0,rulesDeploys:0,cloudRunDeploys:0,storageWrites:0,hrWrites:0,legacyWrites:0,production:false,merge:false};
          fs.writeFileSync('.tmp/corte6-credential-continuity-hosting/site/corte6-protected-runtime-proof.json',JSON.stringify(proof,null,2)+'\n','utf8');
          NODE

      - name: Deploy once through official Firebase Hosting REST API
        id: deploy
        env:
          CXORBIA_EXPECTED_PROJECT: cxorbia-backend-dev
          CXORBIA_EXPECTED_SITE: cxorbia-backend-dev
          CXORBIA_HOSTING_OUT_DIR: .tmp/corte6-credential-continuity-hosting
          CXORBIA_HOSTING_SITE_DIR: .tmp/corte6-credential-continuity-hosting/site
          CXORBIA_HOSTING_CONFIG: .tmp/corte6-credential-continuity-hosting/firebase.hosting.json
          CXORBIA_HOSTING_PREFLIGHT: .tmp/corte6-credential-continuity-hosting/preflight.source-safe.json
          CXORBIA_HOSTING_DEPLOY_REPORT: .tmp/corte6-credential-continuity-hosting/direct-deploy.source-safe.json
          CXORBIA_EXECUTE_HOSTING: 'true'
        run: |
          set -euo pipefail
          node tools/release/cxorbia-existing-hosting-dev-direct-deploy.mjs

      - name: Verify remote protected runtime package + session continuity
        id: remote
        run: |
          set -euo pipefail
          BASE='https://cxorbia-backend-dev.web.app'
          curl -fsSL --retry 5 --retry-delay 2 "$BASE/core/backend-config-preview-dev.js" -o /tmp/preview.js
          curl -fsSL --retry 5 --retry-delay 2 "$BASE/core/backend-protected-dev-mode.js" -o /tmp/protected.js
          curl -fsSL --retry 5 --retry-delay 2 "$BASE/core/backend-protected-dev-session-continuity.js" -o /tmp/session.js
          curl -fsSL --retry 5 --retry-delay 2 "$BASE/core/backend-browser-auth.js" -o /tmp/auth.js
          curl -fsSL --retry 5 --retry-delay 2 "$BASE/core/backend-firebase.js" -o /tmp/firebase.js
          curl -fsSL --retry 5 --retry-delay 2 "$BASE/index-backend-dev.html" -o /tmp/index.html
          curl -fsSL --retry 5 --retry-delay 2 "$BASE/corte6-protected-runtime-proof.json" -o /tmp/proof.json
          grep -q "persist:'local'" /tmp/protected.js
          grep -q "reuseAuthenticatedSession:true" /tmp/protected.js
          grep -q "Persistence.LOCAL" /tmp/session.js
          grep -q "Persistence.SESSION" /tmp/session.js
          grep -q "credentialsEmbedded:false" /tmp/session.js
          grep -q "backend-protected-dev-session-continuity.js" /tmp/index.html
          grep -q "SHOPPER_SCOPE_REQUIRED" /tmp/auth.js
          grep -q "loadAuthorizedShoppers" /tmp/firebase.js
          grep -q "submitida" /tmp/protected.js
          node <<'NODE'
          const fs=require('fs');const p=JSON.parse(fs.readFileSync('/tmp/proof.json','utf8'));
          if(p.schemaVersion!=='cxorbia.corte6-protected-runtime-hosting-proof.v2'||p.firebaseProjectId!=='cxorbia-backend-dev'||p.hostingSite!=='cxorbia-backend-dev'||p.protectedRuntime!==true||p.authMode!=='firebase-auth-custom-claims-rbac'||p.sessionContinuity!=='firebase-local-after-first-real-auth'||p.readOnly!==true||p.writeMode!=='disabled'||p.production!==false)throw new Error('remote_proof_mismatch');
          const d=JSON.parse(fs.readFileSync('.tmp/corte6-credential-continuity-hosting/direct-deploy.source-safe.json','utf8'));
          const e={schemaVersion:'cxorbia.corte6-protected-session-continuity-hosting-deploy.v1',generatedAt:new Date().toISOString(),decision:'PASS_EXISTING_HOSTING_DEV_PROTECTED_SESSION_CONTINUITY_REMOTE_VERIFIED',authorizationId:p.authorizationId,projectId:'cxorbia-backend-dev',hostingSite:'cxorbia-backend-dev',hostingTarget:'cxorbia-dev',versionName:d.versionName,releaseName:d.releaseName,hostingDeployExecutions:1,remote:{protectedRuntimeAssets:true,authBridge:true,firestoreAdapter:true,profileBridge:true,historyKpiIncludesSubmitted:true,sourceSafeDefaultPreserved:true,sessionContinuityAsset:true,persistenceLocal:true,credentialsEmbedded:false},safety:{firestoreWrites:0,authWrites:0,rulesDeploys:0,cloudRunDeploys:0,storageWrites:0,hrWrites:0,legacyWrites:0,makeWrites:0,geminiCalls:0,paymentsWrites:0,production:false,merge:false,piiExported:false,secretsExported:false}};
          fs.writeFileSync('/tmp/c6-protected-hosting-evidence.json',JSON.stringify(e,null,2)+'\n','utf8');
          NODE

      - name: Persist sanitized evidence and consume one-shot authorization
        run: |
          set -euo pipefail
          git fetch origin docs-tya-v6-v71-audit
          git reset --hard origin/docs-tya-v6-v71-audit
          mkdir -p app/docs/evidence
          cp /tmp/c6-protected-hosting-evidence.json app/docs/evidence/CORTE6-PROTECTED-RUNTIME-HOSTING-DEPLOY-LATEST.json
          node <<'NODE'
          const fs=require('fs');
          const rp='backend/config/corte6-credential-continuity-hosting-request.json';
          const xp='backend/config/corte6-protected-session-continuity-hosting-execute.json';
          const r=JSON.parse(fs.readFileSync(rp,'utf8')),x=JSON.parse(fs.readFileSync(xp,'utf8')),e=JSON.parse(fs.readFileSync('app/docs/evidence/CORTE6-PROTECTED-RUNTIME-HOSTING-DEPLOY-LATEST.json','utf8'));
          if(e.decision!=='PASS_EXISTING_HOSTING_DEV_PROTECTED_SESSION_CONTINUITY_REMOTE_VERIFIED'||e.hostingDeployExecutions!==1)throw new Error('evidence_contract_mismatch');
          for(const o of [r,x]){o.status='consumed_pass';o.enabled=false;o.consumed=true;o.consumedAt=new Date().toISOString();o.decision=e.decision;o.versionName=e.versionName;o.releaseName=e.releaseName;o.nextGate='ONE_REAL_LOGIN_THEN_REFRESH_WITHOUT_REPROMPT_AND_HUMAN_VISUAL';}
          r.hostingDeployExecutions=1;
          fs.writeFileSync(rp,JSON.stringify(r,null,2)+'\n','utf8');
          fs.writeFileSync(xp,JSON.stringify(x,null,2)+'\n','utf8');
          NODE
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/docs/evidence/CORTE6-PROTECTED-RUNTIME-HOSTING-DEPLOY-LATEST.json backend/config/corte6-credential-continuity-hosting-request.json backend/config/corte6-protected-session-continuity-hosting-execute.json
          git commit -m 'evidence: Corte 6 protected session continuity Hosting DEV PASS'
          git push origin HEAD:docs-tya-v6-v71-audit

      - name: Publish status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          STATE=success;DESC='PASS_C6_PROTECTED_SESSION_CONTINUITY_HOSTING'
          if [ '${{ job.status }}' != 'success' ]; then STATE=error;DESC='FAIL_C6_PROTECTED_SESSION_CONTINUITY_HOSTING'; fi
          gh api --method POST -H 'Accept: application/vnd.github+json' "/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" -f state="$STATE" -f context="cxorbia/corte6-protected-session-continuity-hosting/$DESC" -f description="$DESC" >/dev/null

      - name: Cleanup
        if: always()
        run: rm -rf .tmp/c6-credential-continuity-hosting

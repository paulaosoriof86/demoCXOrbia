name: CXOrbia C6 Equivalent Universe Provider Readonly v2.2 — Frozen

on:
  push:
    branches:
      - docs-tya-v6-v71-audit
    paths:
      - backend/config/corte6-shopper-equivalent-universe-provider-readonly-v22-request-retired.json

permissions:
  contents: read
  statuses: write

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'One-shot provider read-only run 31104541809 completed with STOP_RETRY. New explicit authorization and a new immutable request are required.'

name: CXOrbia Existing Runtime — F5 Synthetic Acceptance One-Shot — CONSUMED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  f5-consumed:
    runs-on: ubuntu-latest
    steps:
      - name: F5 one-shot permanently inert
        run: |
          echo 'F5_LIVE_SYNTHETIC_ACCEPTANCE already CLOSED/PASS.'
          echo 'Execution CXORBIA-F5-EXECUTE-20260827-01 is consumed and cannot replay.'
          echo 'No Cloud Build, Cloud Run update, Hosting deploy, provider write or retry is executed by this workflow.'

name: CXOrbia C6 Shopper Group Provenance Residual Identity Source Only — Frozen

on:
  push:
    branches:
      - docs-tya-v6-v71-audit
    paths:
      - backend/config/corte6-shopper-group-provenance-source-only-request-retired.json

permissions:
  contents: read
  statuses: write

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Source-only group provenance block completed; a new explicit authorization and workflow restoration are required.'

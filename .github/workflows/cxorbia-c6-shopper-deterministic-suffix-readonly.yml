name: CXOrbia C6 Shopper Diagnostic Contract Root Fix Source Only — Frozen

on:
  push:
    branches:
      - docs-tya-v6-v71-audit
    paths:
      - backend/config/corte6-shopper-deterministic-suffix-readonly-request-retired.json

permissions:
  contents: read
  statuses: write

jobs:
  frozen:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Consumed source-only trigger frozen; a new explicit authorization and workflow restoration are required.'

# NOTA-BACKEND-20260629-OPERACION

Se creó `app/core/backend-operational-actions.js` como archivo nuevo de backend.

El archivo prepara estos métodos:

```text
performOperationAction
getOperationActions
getEntityAuditTrail
```

Queda pendiente conectarlo únicamente en preview DEV cuando Paula autorice.

No se tocó producción, no se publicó Hosting, no se publicó reglas y no se modificó `/app/modules`.

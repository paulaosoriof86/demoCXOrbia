# Pendientes del prototipo — addendum tras V102 inválido

## Nuevo P0 de proceso

Agregar gate automático de identidad de proyecto antes de aceptar cualquier candidata Claude:

1. raíz esperada `app/`;
2. `app/index.html`, `app/app.js`, `app/core/`, `app/modules/` presentes;
3. namespace `window.CX` y registros `CX.module(...)`;
4. manifest con repo `paulaosoriof86/demoCXOrbia`;
5. cero `orbit360-platform`, `window.Orbit` y dominios funcionales de seguros;
6. comparación obligatoria contra la última candidata CXOrbia aceptada.

## Pendientes funcionales vigentes

Se mantienen sin cambios los bloques A–F de la auditoría V101: fixtures, copy de integraciones, permisos contextuales, Academia/Certificación, source lock y smoke reproducible.

El supuesto V102 no resolvió ningún pendiente CXOrbia porque no contiene código CXOrbia.
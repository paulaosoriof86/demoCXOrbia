# PENDIENTES PROTOTIPO — C6 AUTH PRINCIPAL-UNIQUENESS PHASE 1 DIGEST HOLD

## Pendiente vivo

1. Canonicalizar el annotation audit-only del row `ac93d90d9e41512acdcd` para que transformador, freeze v3 y contrato v2 calculen el mismo digest.
2. Reejecutar únicamente FASE 1 source-only.
3. Solo con FASE 1 PASS, emitir request provider nuevo/no solapado para PREWRITE + Auth Activation DEV.

## No reabrir

- lineage del target: PASS `profile + visit`;
- SKIP13: cerrado `13/13`;
- multi-Auth: adjudicado y cerrado;
- investigación password de `ac93...` bajo `UPDATE_AUTH`: obsoleta por causa raíz cross-row principal alias.

No hay ajuste frontend pendiente generado por este bloque.

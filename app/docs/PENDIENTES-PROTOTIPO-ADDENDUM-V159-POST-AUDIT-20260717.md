# PENDIENTES PROTOTIPO — V159 POST AUDITORÍA

Fecha: 2026-07-17

## Pendiente inmediato único

Aplicar físicamente V159 mediante `APPLY_DELTA_DIRECTLY` sobre `docs-tya-v6-v71-audit`, preservando backend, overlays, documentación y reconciliaciones protegidas.

Después:

1. generar manifest, build-lock y verificador de la unión;
2. ejecutar gates proyecto/periodo/HR/shoppers/certificaciones/finanzas;
3. ejecutar smoke Admin, Shopper y Cliente;
4. realizar validación visual;
5. congelar baseline y continuar Phase A operativa.

## No pendientes válidos

- no solicitar V160;
- no reauditar V159 completa;
- no crear rama, PR, workflow o transportador;
- no usar `incoming/`, Base64, blobs/trees, Drive o PowerShell;
- no pedir copias manuales a Paula;
- no convertir copy P1/P2 en nuevo P0.

## P1/P2 post-empalme

- Normalizar el encabezado histórico V82 que aún aparece al inicio de `PENDIENTES-PROTOTIPO.md`, aunque el mismo documento ya declara V159.
- Validar visualmente vocabulario comercial y acceso técnico protegido en Academia.
- Ejecutar el smoke de navegador que no pudo completarse en el entorno de auditoría por cierre del proceso GPU de Chromium.

## Reconciliaciones protegidas

- Importador debe usar `CX.dataSource.sourceContract()`.
- Finanzas conserva `data.project()` y adapter `project()/period()/visitas()`.
- TyA/Cinépolis permanece overlay configurable, no lógica global.

## Estado seguro

Sin empalme físico V159, merge, deploy, producción, imports reales, writes, proveedores live ni pagos.
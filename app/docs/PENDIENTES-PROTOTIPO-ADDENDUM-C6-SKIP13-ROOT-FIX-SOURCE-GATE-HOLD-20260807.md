# PENDIENTES PROTOTIPO — C6 SKIP13 root-fix source-gate HOLD

Pendiente técnico inmediato:

```text
fix=eliminar side effect argv --self-test entre adjudicador v2 y cxorbia-c6-shopper-equivalent-universe.mjs
providerAttemptAllowed=false
```

Mantener:

- `profileFingerprintNamespace=deterministic-suffix-plan-profile`;
- `authCandidateFingerprintNamespace=shopper-auth-candidate-v1`;
- prohibición de `shopper-collision-member-v1` y `multi-auth-profile-v1` como join key SKIP13;
- 13 fingerprints congelados sin cambios;
- direct runner DEV PASS y provider boundary OFF.

Después del PASS source-only se requerirá autorización nueva para una única adjudicación provider read-only. No reutilizar run `31190357507` ni workflow v2 retirado.

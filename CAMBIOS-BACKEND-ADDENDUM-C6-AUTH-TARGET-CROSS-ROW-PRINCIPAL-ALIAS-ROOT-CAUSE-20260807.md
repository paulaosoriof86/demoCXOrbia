# CAMBIOS BACKEND — C6 AUTH TARGET CROSS-ROW PRINCIPAL ALIAS ROOT CAUSE

Ver fuente canónica: `app/docs/CAMBIOS-BACKEND-ADDENDUM-C6-AUTH-TARGET-CROSS-ROW-PRINCIPAL-ALIAS-ROOT-CAUSE-20260807.md`.

Resultado rector: lineage target PASS con bases `profile + visit`; `candidateCount=0` target-specific; causa raíz demostrada `CROSS_ROW_EXISTING_AUTH_PRINCIPAL_ALIAS_IN_OLD_PREWRITE`; cero writes; plan Auth previo no ejecutable hasta root fix source-only de principal uniqueness.

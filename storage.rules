rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Storage queda cerrado hasta activar Blaze y definir migración de evidencias.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

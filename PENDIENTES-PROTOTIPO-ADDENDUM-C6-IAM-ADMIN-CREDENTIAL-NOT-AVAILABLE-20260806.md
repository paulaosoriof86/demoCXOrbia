# PENDIENTES PROTOTIPO — C6 IAM ADMIN credential not available

```text
decision=ADMIN_CREDENTIAL_CONFIGURATION_REQUIRED
requiredTarget=cxorbia-c6-runner-dev@cxorbia-backend-dev.iam.gserviceaccount.com
```

Debe configurarse un mecanismo administrativo válido para `cxorbia-backend-dev` sin exponer secretos. No reutilizar Default Compute, Firebase Admin SDK, el request consumido, el workflow retirado, el run `31133874657` ni el job `92728797539`.

# RESUMEN-PARA-CLAUDE.md

**Última actualización:** 2026-08-11  
**Estado:** `STOP_RETRY_C6_STAFF_REPAIR_BOOTSTRAP_EXACT_WRITE__PRIVATE_VISIBLE_LOGIN_UNRESOLVED_B__AUTH_WRITES_0__FIRESTORE_WRITES_0__NO_DELETE__NO_DEPLOY__NO_PRODUCTION`

No reabrir frontend acumulativo, Auth 228, Activation/readback/rollback, SKIP13, MultiAuth, HashConfig, direct runner, M4, HR M6, static gate ni provider snapshot PASS.

Exact write autorizado se detuvo antes del primer provider write: blocker `PRIVATE_VISIBLE_LOGIN_UNRESOLVED_B`; credentialPrivacyPass=true; identityResolutionPass=false; Auth writes=0; Firestore writes=0; deletes=0. A, R4 y ocho históricos sin mutación.

Claude: **no crear pantalla nueva, no rediseñar, no fallback local y no hardcodear B**. `app/modules/configuracion.js#usuarios` sigue sin tocar desde backend. Wiring localizado solo después de bootstrap/readback PASS. Mantener alta `TyA completo|Proyectos específicos`, scope editable desde inventario vivo, no exponer claims/fingerprints/provider IDs, alta/edición/disable/reactivate con readback y sin hard delete.

M6 HR COMPLETE. **Phase A 84%; restante 16%; M5=4/8.**

Siguiente backend: `C6 STAFF TARGET PRIVATE IDENTITY RECOVERY SOURCE-ONLY`. Claude no debe adelantarse ni crear identidad sustituta.

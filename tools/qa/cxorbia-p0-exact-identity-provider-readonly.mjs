#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import vm from 'node:vm';

const args=new Set(process.argv.slice(2));
const selfTest=args.has('--self-test');
const requestPath=process.argv.find(x=>!x.startsWith('--'))||'backend/config/corte6-human-login-shopper-identity-audit.json';
const credentialPath=process.env.GOOGLE_APPLICATION_CREDENTIALS||'';
const remoteRoot=String(process.env.CXORBIA_DEV_ROOT_URL||'https://cxorbia-backend-dev.web.app').replace(/\/$/,'');
const outPath=process.env.CXORBIA_P0_PROVIDER_AUDIT_OUTPUT||'.tmp/c6-human-login-shopper-identity-audit/p0-provider-universe.source-safe.json';
const tenantId='tya',projectId='cinepolis',expectedProject='cxorbia-backend-dev';
const contractPath='app/adapters/cxorbia-exact-identity-contract-v1.js';
const composerPath='app/adapters/tya-cumulative-read-model-v2.js';
const normalizerPath='app/adapters/cxorbia-exact-linked-owner-normalizer-v1.js';
const str=v=>String(v==null?'':v).trim();
const list=v=>Array.isArray(v)?v.map(str).filter(Boolean):(typeof v==='string'?v.split(',').map(str).filter(Boolean):[]);
const hash=v=>crypto.createHash('sha256').update(String(v),'utf8').digest('hex').slice(0,16);
const clone=v=>v==null?v:JSON.parse(JSON.stringify(v));
const docData=d=>({id:d.id,...(d.data()||{})});
function fail(code,detail=''){const e=new Error(detail?`${code}:${detail}`:code);e.code=code;throw e;}
function safeClaims(claims={}){const role=str(claims.role).toLowerCase();const ns=str(claims.authNamespace||(role==='shopper'?'shopper':'staff')).toLowerCase();return{role,ns,tenantOk:claims.tenantId===tenantId||list(claims.tenants).includes(tenantId),projectOk:claims.projectId===projectId||list(claims.projectIds).includes(projectId),shopperId:str(claims.shopperId)};}
async function allAuthUsers(auth){const rows=[];let token;do{const page=await auth.listUsers(1000,token);rows.push(...page.users);token=page.pageToken;}while(token);return rows;}
async function allDocs(ref){const snap=await ref.get();return snap.docs.map(docData);}
async function liveHr(){const url=remoteRoot+'/api/tya/cinepolis/hr-live?view=operational-names&cxOperationalPreview=YES_PAULA_20260731_NAMES_DEV&format=json&fresh=1&ts='+Date.now();const response=await fetch(url,{headers:{'cache-control':'no-cache, no-store','pragma':'no-cache'}});if(!response.ok)fail('HR_LIVE_HTTP_'+response.status);const payload=await response.json();const snapshot=payload?.snapshot||payload?.data||payload;const periods=Array.isArray(snapshot?.periods)?snapshot.periods:[];const visits=Array.isArray(snapshot?.visits)?snapshot.visits:[];const shoppers=Array.isArray(snapshot?.shoppers)?snapshot.shoppers:[];if(!periods.length||!visits.length||!shoppers.length)fail('HR_LIVE_REQUIRED_ARRAYS_EMPTY');return{snapshot,periods,visits,shoppers,posts:Array.isArray(snapshot.posts)?snapshot.posts:(Array.isArray(snapshot._posts)?snapshot._posts:[]),runtime:payload?._runtime||snapshot?._runtime||{}};}
function loadRuntime(){const sandbox={console:{log(){},warn(){},error(){}},setTimeout,clearTimeout};sandbox.window=sandbox;sandbox.globalThis=sandbox;vm.createContext(sandbox);vm.runInContext(fs.readFileSync(contractPath,'utf8'),sandbox,{filename:contractPath,timeout:3000});vm.runInContext(fs.readFileSync(composerPath,'utf8'),sandbox,{filename:composerPath,timeout:3000});vm.runInContext(fs.readFileSync(normalizerPath,'utf8'),sandbox,{filename:normalizerPath,timeout:3000});const contract=sandbox.CX_EXACT_IDENTITY_CONTRACT,composer=sandbox.CX_TYA_CUMULATIVE_READ_MODEL,normalizer=sandbox.CX_EXACT_LINKED_OWNER_NORMALIZER;if(!contract||contract.version!=='cxorbia-exact-identity-contract-v1')fail('IDENTITY_CONTRACT_NOT_LOADED');if(!composer||typeof composer.compose!=='function')fail('COMPOSER_NOT_LOADED');if(!normalizer||normalizer.installed!==true)fail('LINKED_OWNER_NORMALIZER_NOT_LOADED');return{contract,composer,normalizer};}
function relevantReview(review,claim){return(Array.isArray(review)?review:[]).some(item=>item&&typeof item==='object'&&(str(item.liveShopperId)===claim||str(item.profileId)===claim||str(item.shopperId)===claim||(Array.isArray(item.candidates)&&item.candidates.map(str).includes(claim))||(Array.isArray(item.shopperIds)&&item.shopperIds.map(str).includes(claim))));}
function composeAll(composer,hr,profiles,protectedVisits,posts,liquidations,certifications){return composer.compose({hr:{projects:clone(hr.snapshot.projects||hr.periods),visits:clone(hr.visits),shoppers:clone(hr.shoppers),posts:clone(hr.posts),currentProjectId:projectId,currentPeriodId:str(hr.snapshot.currentPeriodId||hr.snapshot.currentProjectId||''),sourceRevision:str(hr.runtime.revision||hr.snapshot.sourceRevision||'')},protectedPayload:{visits:clone(protectedVisits),shoppers:clone(profiles),posts:clone(posts),certifications:clone(certifications),liquidations:clone(liquidations)}});}

function runSelfTest(){const{composer}=loadRuntime();const hr={snapshot:{projects:[{id:'period-1'}],currentPeriodId:'period-1'},periods:[{id:'period-1'}],visits:[{id:'live-v1',hrRowId:'row-1',shopperId:'live-s1'}],shoppers:[{id:'live-s1'}],posts:[],runtime:{}};const profiles=[{id:'profile-1',shopperId:'profile-1',legacyId:'legacy-1'}];const protectedVisits=[{id:'protected-v1',shopperId:'legacy-1',hrRowId:'row-1'}];const result=composeAll(composer,hr,profiles,protectedVisits,[],[],[]);const map=result.identityMap||{};const pass=Object.values(map).map(str).includes('profile-1')&&(result.shoppers||[]).some(x=>str(x.id||x.shopperId)==='profile-1')&&(result.visits||[]).some(x=>str(x.shopperId)==='profile-1')&&result.identityOwnerNormalizerVersion==='cxorbia-exact-linked-owner-normalizer-v1';console.log(JSON.stringify({schemaVersion:'cxorbia.p0.exact-identity-provider-readonly.selftest.v2',decision:pass?'PASS_P0_GLOBAL_COMPOSITION_SELFTEST':'FAIL_P0_GLOBAL_COMPOSITION_SELFTEST',testedChain:'profile_claim_to_profile_alias_to_protected_visit_to_hr_row_to_live_hr',preClassificationScoping:false,linkedOwnerNormalizer:result.identityOwnerNormalizerVersion||null,providerReads:0,writes:0,deploys:0}));if(!pass)process.exitCode=1;}

async function main(){const admin=(await import('firebase-admin')).default;const request=JSON.parse(fs.readFileSync(requestPath,'utf8'));if(request.mode!=='p0_exact_identity_provider_readonly_and_real_e2e')fail('REQUEST_MODE_INVALID');if(request.enabled!==true||request.consumed!==false||Number(request.allowedExecutions)!==1)fail('REQUEST_NOT_ONE_SHOT_ENABLED');if(request.providerReads!==true||request.providerWrites!==false)fail('REQUEST_NOT_READONLY');if(request.canonicalIdentityContract!==contractPath)fail('REQUEST_CONTRACT_MISMATCH');if(!credentialPath||!fs.existsSync(credentialPath))fail('SERVICE_ACCOUNT_FILE_MISSING');const serviceAccount=JSON.parse(fs.readFileSync(credentialPath,'utf8'));if(serviceAccount.project_id!==expectedProject||!serviceAccount.private_key)fail('SERVICE_ACCOUNT_TARGET_MISMATCH');if(!admin.apps.length)admin.initializeApp({credential:admin.credential.cert(serviceAccount),projectId:expectedProject});const auth=admin.auth(),db=admin.firestore();const{contract,composer}=loadRuntime();
  const[users,profiles,protectedVisits,postulations,applications,liquidations,certifications,hr]=await Promise.all([allAuthUsers(auth),allDocs(db.collection('tenants').doc(tenantId).collection('shoppers')),allDocs(db.collection('tenants').doc(tenantId).collection('projects').doc(projectId).collection('visits')),allDocs(db.collection('tenants').doc(tenantId).collection('projects').doc(projectId).collection('postulations')),allDocs(db.collection('tenants').doc(tenantId).collection('projects').doc(projectId).collection('applications')),allDocs(db.collection('tenants').doc(tenantId).collection('projects').doc(projectId).collection('liquidations')),allDocs(db.collection('tenants').doc(tenantId).collection('projects').doc(projectId).collection('certifications')),liveHr()]);
  const profileById=new Map(profiles.map(p=>[str(p.id||p.shopperId),p]).filter(([id])=>id));const effective=[];for(const user of users){const c=safeClaims(user.customClaims||{});if(c.role==='shopper'&&c.ns==='shopper'&&c.tenantOk&&c.projectOk&&c.shopperId)effective.push({uid:user.uid,claim:c.shopperId});}if(!effective.length)fail('NO_EFFECTIVE_SHOPPER_PRINCIPALS');
  const composed=composeAll(composer,hr,profiles,protectedVisits,[...postulations,...applications],liquidations,certifications);const mapValues=new Set(Object.values(composed.identityMap||{}).map(str).filter(Boolean));const outcome={unique:0,missingProfile:0,unmapped:0,ambiguousOrReview:0,withHistory:0,withoutHistory:0};const issues=[];
  for(const principal of effective){if(!profileById.has(principal.claim)){outcome.missingProfile++;issues.push({fingerprint:hash(principal.uid),code:'CLAIM_PROFILE_MISSING'});continue;}const ownRows=(composed.shoppers||[]).filter(row=>str(row.id||row.shopperId)===principal.claim);const ownHistory=(composed.visits||[]).filter(row=>str(row.shopperId)===principal.claim).length;const inReview=relevantReview(composed.identityReviewQueue,principal.claim);const hasHrCrosswalk=mapValues.has(principal.claim);if(ownRows.length===1&&hasHrCrosswalk&&!inReview){outcome.unique++;if(ownHistory>0)outcome.withHistory++;else outcome.withoutHistory++;}else if(inReview||ownRows.length>1){outcome.ambiguousOrReview++;issues.push({fingerprint:hash(principal.uid),code:'AMBIGUOUS_OR_REVIEW'});}else{outcome.unmapped++;issues.push({fingerprint:hash(principal.uid),code:'NO_EXACT_HR_CROSSWALK'});}}
  const pass=outcome.unique===effective.length&&outcome.missingProfile===0&&outcome.unmapped===0&&outcome.ambiguousOrReview===0;const report={schemaVersion:'cxorbia.p0.exact-identity-provider-readonly.result.v2',generatedAt:new Date().toISOString(),decision:pass?'PASS_P0_EXACT_IDENTITY_PROVIDER_UNIVERSE':'HOLD_P0_EXACT_IDENTITY_PROVIDER_UNIVERSE',contractVersion:contract.version,linkedOwnerNormalizerVersion:composed.identityOwnerNormalizerVersion||null,technicalKeyCount:contract.technicalKeys.length,classificationMode:'global_composition_before_principal_classification',universe:{authUsersReviewed:users.length,effectiveShopperPrincipals:effective.length,firestoreProfiles:profiles.length,protectedVisits:protectedVisits.length,postulations:postulations.length,applications:applications.length,liquidations:liquidations.length,certifications:certifications.length,hrPeriods:hr.periods.length,hrVisits:hr.visits.length,hrShoppers:hr.shoppers.length},mapping:outcome,identityMapEntries:Object.keys(composed.identityMap||{}).length,reviewQueueEntries:(composed.identityReviewQueue||[]).length,issueCount:issues.length,issues:issues.slice(0,50),piiExported:false,secretsExported:false,safety:{providerReads:true,providerWrites:0,authWrites:0,firestoreWrites:0,hrWrites:0,rulesWrites:0,storageWrites:0,passwordChanges:0,passwordResets:0,deploys:0,merge:false,production:false}};fs.mkdirSync(path.dirname(outPath),{recursive:true});fs.writeFileSync(outPath,JSON.stringify(report,null,2)+'\n','utf8');console.log(JSON.stringify(report,null,2));if(!pass)process.exitCode=2;}

if(selfTest)runSelfTest();else main().catch(error=>{const report={schemaVersion:'cxorbia.p0.exact-identity-provider-readonly.result.v2',generatedAt:new Date().toISOString(),decision:'FAIL_P0_EXACT_IDENTITY_PROVIDER_UNIVERSE_EXECUTION',errorCode:str(error?.code||error?.message||'UNKNOWN').replace(/[^A-Za-z0-9_.:-]+/g,'_').slice(0,180),piiExported:false,secretsExported:false,safety:{providerReads:true,providerWrites:0,authWrites:0,firestoreWrites:0,hrWrites:0,rulesWrites:0,storageWrites:0,passwordChanges:0,passwordResets:0,deploys:0,merge:false,production:false}};fs.mkdirSync(path.dirname(outPath),{recursive:true});fs.writeFileSync(outPath,JSON.stringify(report,null,2)+'\n','utf8');console.log(JSON.stringify(report,null,2));process.exitCode=3;});

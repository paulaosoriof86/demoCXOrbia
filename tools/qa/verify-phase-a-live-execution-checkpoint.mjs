#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import {execFileSync} from 'node:child_process';
import {fileURLToPath} from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url));
const repo=path.resolve(here,'../..');
const read=p=>fs.readFileSync(path.join(repo,p),'utf8').replace(/^\uFEFF/,'');
const json=p=>JSON.parse(read(p));
const ensure=(v,c)=>{if(!v)throw new Error(c);};
const lock=json('backend/config/cxorbia-phase-a-continuity-lock.json');
const tomb=json('backend/config/cxorbia-historical-authority-tombstones.json');
const q=json('backend/config/cxorbia-m3-quiescence-lock.json');
const current=Number(tomb.progress?.currentResidualHolds), queueNext=`M3_F1_FINITE_TOMBSTONE_QUEUE_REMAINING_${current}`;
const barrierActive=q.status!=='CLOSED_PASS', expectedNext=barrierActive?'M3_0_FINAL_CLEAN_PROBE_AND_CLOSE_OR_FAIL_CLOSED':queueNext;
ensure(lock.repository==='paulaosoriof86/demoCXOrbia','CURRENT_REPOSITORY_DRIFT');
ensure(lock.branch==='docs-tya-v6-v71-audit'&&Number(lock.pullRequest)===7,'CURRENT_LANE_DRIFT');
ensure(lock.masterPlan?.id==='CXORBIA-MASTER-GO-LIVE-POSTPROD-RC15-V1'&&lock.masterPlan?.status==='FROZEN','MASTER_PLAN_DRIFT');
ensure(lock.productionState?.functionalSourceLock==='f9802fdd498934a8e7729fa5c7d18341bec1cd71','FUNCTIONAL_SOURCE_LOCK_DRIFT');
ensure(lock.currentIteration==='I5-G2','CURRENT_ITERATION_NOT_G2');
ensure(Number(lock.formalProgress?.completed)===98&&Number(lock.productionRealReadiness?.completed)===68,'CURRENT_PROGRESS_METRICS_DRIFT');
ensure(lock.productionState?.active===true&&lock.productionState?.productionUrl==='https://cxorbia-backend-dev.web.app','PRODUCTION_TARGET_DRIFT');
ensure(lock.productionState?.providerRedeployExecuted===false&&lock.productionState?.rebuildExecuted===false&&lock.productionState?.businessDataWritesAuthorized===false,'PRODUCTION_SAFETY_DRIFT');
ensure(lock.m1ExecutionControl?.status==='CLOSED_PASS'&&lock.m2ExecutionControl?.status==='CLOSED_PASS','M1_M2_STATE_DRIFT');
ensure(lock.m3ExecutionControl?.providerMutationAuthorizedNow===false,'M3_PROVIDER_AUTHORITY_DRIFT');
ensure(Number(lock.m3ExecutionControl?.currentResidualHolds)===current,'M3_RESIDUAL_COUNT_DRIFT');
ensure(lock.masterPlan?.next===expectedNext&&lock.m3ExecutionControl?.next===expectedNext,'M3_NEXT_DRIFT');
if(barrierActive){ensure(lock.m3ExecutionControl?.status==='PAUSED_M3_0_QUIESCENCE_BARRIER','M3_BARRIER_STATUS_DRIFT');ensure(lock.m3ExecutionControl?.finiteQueueFrozen===true&&current===27,'M3_QUEUE_NOT_FROZEN');}
const run=(script,token)=>{const out=execFileSync(process.execPath,[script],{cwd:repo,encoding:'utf8'});ensure(out.includes(token),`GATE_NOT_PASS:${script}:${token}`);return out;};
run('tools/continuity/validate-cxorbia-master-plan-freeze-m3.js','MASTER_PLAN_FREEZE_M3_PASS');
run('tools/continuity/validate-cxorbia-canonical-authority.js','CANONICAL_AUTHORITY_GATE_PASS');
run('tools/continuity/validate-cxorbia-m3-quiescence.js','M3_QUIESCENCE_SOURCE_GATE_PASS');
run('tools/continuity/validate-cxorbia-state-sync-m3.js','STATE_SYNC_M3_GATE_PASS');
run('tools/continuity/validate-cxorbia-phase-a-continuity-lock-m3.js','CONTINUITY_M3_ATOMIC_SYNC_PASS');
console.log(JSON.stringify({ok:true,decision:barrierActive?'PASS_PHASE_A_M3_0_BARRIER_SOURCE_CONTRACT':'PASS_PHASE_A_M3_CURRENT_OPERATIONAL_CHECKPOINT',phaseA:{completed:98,pending:2,total:100},productionRealReadiness:{completed:68,pending:32,total:100},functionalSourceLock:lock.productionState.functionalSourceLock,m1:'CLOSED_PASS',m2:'CLOSED_PASS',m3:{status:lock.m3ExecutionControl.status,currentResidualHolds:current,next:expectedNext},safety:{providerWrites:false,businessDataWrites:false,externalHrWrites:false,authWrites:false,paymentWrites:false,deploy:false,rebuild:false,merge:false}},null,2));

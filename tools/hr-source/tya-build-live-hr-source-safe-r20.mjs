#!/usr/bin/env node
/*
  CXOrbia TyA Phase A — live HR source-safe builder R20.

  Orden de lectura read-only:
  1. Google Sheets API con service account, cuando el gate tiene credencial.
  2. Google Visualization CSV por tab con cache-busting, sin depender del XLSX
     público que demostró entregar una copia desactualizada.

  Descubre tabs mensuales por patrón, aplica un contrato único de columnas y
  genera únicamente referencias/estados source-safe. No incluye nombres,
  teléfono, correo, observaciones, URLs privadas ni el workbook crudo.
*/
import crypto from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';
import process from 'node:process';
import {
  applyCanonicalVisitState,
  summarizeCanonicalPeriods,
  validateCanonicalHistory,
  normalizedText,
  validIsoDate
} from './tya-canonical-visit-state-r20.mjs';

const SHEET_ID=process.env.CXORBIA_HR_LIVE_SHEET_ID||'1h307t37LxM1nZNh_9Odt6wHUQhROG6cYbsbMKr48vU4';
const OUT_FILE=process.env.CXORBIA_HR_SOURCE_SAFE_OUT||'app/data/tya-hr-source-safe-periods.js';
const SERVICE_ACCOUNT_JSON=process.env.FIREBASE_SERVICE_ACCOUNT_JSON||'';
const MAX_ROW=Number(process.env.CXORBIA_HR_LIVE_MAX_ROW||140);
const MAX_COL=process.env.CXORBIA_HR_LIVE_MAX_COL||'AI';
const EARLIEST_PERIOD=process.env.CXORBIA_HR_EARLIEST_PERIOD||'2025-01';
const COLUMN_MAP_FILE=path.resolve('backend/contracts/tya-hr-column-map-r20-v1.json');
const GATE_OUT=path.resolve(process.env.CXORBIA_GATE_OUT||'.tmp/r20-source-safe-gates');
const GENERATED_AT=new Date().toISOString();

const MONTHS=[
  {n:1,name:'ENERO',short:'ENE',full:'Enero'}, {n:2,name:'FEBRERO',short:'FEB',full:'Febrero'},
  {n:3,name:'MARZO',short:'MAR',full:'Marzo'}, {n:4,name:'ABRIL',short:'ABR',full:'Abril'},
  {n:5,name:'MAYO',short:'MAY',full:'Mayo'}, {n:6,name:'JUNIO',short:'JUN',full:'Junio'},
  {n:7,name:'JULIO',short:'JUL',full:'Julio'}, {n:8,name:'AGOSTO',short:'AGO',full:'Agosto'},
  {n:9,name:'SEPTIEMBRE',short:'SEP',full:'Septiembre'}, {n:10,name:'OCTUBRE',short:'OCT',full:'Octubre'},
  {n:11,name:'NOVIEMBRE',short:'NOV',full:'Noviembre'}, {n:12,name:'DICIEMBRE',short:'DIC',full:'Diciembre'}
];

function fail(message){throw new Error(message);}
function pad(value){return String(value).padStart(2,'0');}
function safeHash(value,prefix){const raw=String(value||'').trim().toLowerCase();return raw?`${prefix}_${crypto.createHash('sha256').update(raw).digest('hex').slice(0,10)}`:'';}
function b64url(input){return Buffer.from(input).toString('base64').replace(/=/g,'').replace(/\+/g,'-').replace(/\//g,'_');}
function serviceAccountInfo(){try{const value=SERVICE_ACCOUNT_JSON?JSON.parse(SERVICE_ACCOUNT_JSON):{};return {type:value.type||'',projectId:value.project_id||'',clientEmail:value.client_email||''};}catch{return {type:'',projectId:'',clientEmail:''};}}
function signJwt(sa){const now=Math.floor(Date.now()/1000);const unsigned=`${b64url(JSON.stringify({alg:'RS256',typ:'JWT'}))}.${b64url(JSON.stringify({iss:sa.client_email,scope:'https://www.googleapis.com/auth/spreadsheets.readonly',aud:'https://oauth2.googleapis.com/token',iat:now,exp:now+3600}))}`;const signer=crypto.createSign('RSA-SHA256');signer.update(unsigned);signer.end();return `${unsigned}.${signer.sign(sa.private_key).toString('base64').replace(/=/g,'').replace(/\+/g,'-').replace(/\//g,'_')}`;}
async function oauthToken(){if(!SERVICE_ACCOUNT_JSON)fail('Missing FIREBASE_SERVICE_ACCOUNT_JSON env.');const sa=JSON.parse(SERVICE_ACCOUNT_JSON);if(sa.type!=='service_account')fail('Secret is not a service_account JSON.');const body=new URLSearchParams({grant_type:'urn:ietf:params:oauth:grant-type:jwt-bearer',assertion:signJwt(sa)});const response=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','Cache-Control':'no-store'},body});const json=await response.json();if(!response.ok)fail(`Google OAuth failed: HTTP ${response.status} ${JSON.stringify(json).slice(0,300)}`);return {token:json.access_token,clientEmail:sa.client_email};}
async function sheetsGet(pathAndQuery,token){const response=await fetch(`https://sheets.googleapis.com/v4/spreadsheets/${SHEET_ID}${pathAndQuery}`,{headers:{Authorization:`Bearer ${token}`,'Cache-Control':'no-cache, no-store','Pragma':'no-cache'}});const json=await response.json();if(!response.ok)fail(`Sheets API failed: HTTP ${response.status} ${JSON.stringify(json).slice(0,500)}`);return json;}

function parseTabName(title){const clean=String(title||'').trim().replace(/\s+/g,' ').toUpperCase();const match=clean.match(/^(ENERO|FEBRERO|MARZO|ABRIL|MAYO|JUNIO|JULIO|AGOSTO|SEPTIEMBRE|SETIEMBRE|OCTUBRE|NOVIEMBRE|DICIEMBRE)\s+(\d{2})(?:\s+(HN))?$/);if(!match)return null;const monthName=match[1]==='SETIEMBRE'?'SEPTIEMBRE':match[1];const month=MONTHS.find(item=>item.name===monthName);const year=2000+Number(match[2]),country=match[3]==='HN'?'HN':'GT';return {country,year,month:month.n,monthName:month.full,label:`${month.short} ${year}`,fullLabel:`${month.full} ${year}`,key:`${year}-${pad(month.n)}`,tabTitle:title};}
function candidateTabs(){const [startYear,startMonth]=EARLIEST_PERIOD.split('-').map(Number);const now=new Date();let endYear=now.getUTCFullYear(),endMonth=now.getUTCMonth()+2;if(endMonth>12){endMonth-=12;endYear+=1;}const tabs=[];for(let year=startYear;year<endYear||(year===endYear&&startMonth<=endMonth);year++){const first=year===startYear?startMonth:1;const last=year===endYear?endMonth:12;for(let monthNumber=first;monthNumber<=last;monthNumber++){const month=MONTHS[monthNumber-1],yy=String(year).slice(-2);for(const country of ['GT','HN']){const title=`${month.name} ${yy}${country==='HN'?' HN':''}`;tabs.push({country,year,month:month.n,monthName:month.full,label:`${month.short} ${year}`,fullLabel:`${month.full} ${year}`,key:`${year}-${pad(month.n)}`,tabTitle:title});}}}return tabs;}

function parseCsv(text){const rows=[];let row=[],field='',quoted=false;for(let index=0;index<text.length;index++){const char=text[index];if(quoted){if(char==='"'&&text[index+1]==='"'){field+='"';index++;}else if(char==='"')quoted=false;else field+=char;}else if(char==='"')quoted=true;else if(char===','){row.push(field);field='';}else if(char==='\n'){row.push(field.replace(/\r$/,''));rows.push(row);row=[];field='';}else field+=char;}if(field||row.length){row.push(field.replace(/\r$/,''));rows.push(row);}return rows;}
async function fetchGvizTab(tab){const nonce=`${Date.now()}-${crypto.randomBytes(6).toString('hex')}`;const params=new URLSearchParams({tqx:'out:csv',sheet:tab.tabTitle,range:`A1:${MAX_COL}${MAX_ROW}`,tq:'select *',_cxnonce:nonce});const url=`https://docs.google.com/spreadsheets/d/${SHEET_ID}/gviz/tq?${params}`;const response=await fetch(url,{redirect:'follow',headers:{'Cache-Control':'no-cache, no-store, max-age=0','Pragma':'no-cache','Expires':'0','User-Agent':'CXOrbia-Phase-A-R20/1.0'}});const type=response.headers.get('content-type')||'';const text=await response.text();if(!response.ok||!type.includes('text/csv')||/^<!doctype html/i.test(text.trim())||/google visualization api query language/i.test(text.toLowerCase()))return null;const values=parseCsv(text).slice(0,MAX_ROW);return values.length?{range:tab.tabTitle,values,nonce}:null;}
async function mapLimit(items,limit,mapper){const results=new Array(items.length);let cursor=0;async function worker(){while(true){const index=cursor++;if(index>=items.length)return;try{results[index]=await mapper(items[index],index);}catch(error){results[index]={error:String(error?.message||error)};}}}await Promise.all(Array.from({length:Math.min(limit,items.length)},worker));return results;}

async function readFromSheetsApi(){const {token,clientEmail}=await oauthToken();const metadata=await sheetsGet('?fields=properties(title),sheets(properties(title,index))',token);const tabs=(metadata.sheets||[]).map(sheet=>parseTabName(sheet.properties?.title)).filter(Boolean);if(!tabs.length)fail('No monthly HR tabs detected through Sheets API.');const valueRanges=[];for(let index=0;index<tabs.length;index+=8){const ranges=tabs.slice(index,index+8).map(tab=>`'${tab.tabTitle.replace(/'/g,"''")}'!A1:${MAX_COL}${MAX_ROW}`);const query=ranges.map(range=>`ranges=${encodeURIComponent(range)}`).join('&');const json=await sheetsGet(`/values:batchGet?majorDimension=ROWS&valueRenderOption=FORMATTED_VALUE&${query}`,token);valueRanges.push(...(json.valueRanges||[]));}return {accessMode:'sheets_api_service_account',clientEmail,title:metadata.properties?.title||'HR Guatemala - Sincronizacion Google Sheets',tabs,valueRanges,cacheBustApplied:true};}
async function readFromGvizCsv(apiError){const candidates=candidateTabs();const fetched=await mapLimit(candidates,6,async tab=>({tab,valueRange:await fetchGvizTab(tab)}));const valid=fetched.filter(item=>item?.valueRange&&Array.isArray(item.valueRange.values)&&item.valueRange.values.length);const tabs=valid.map(item=>item.tab),valueRanges=valid.map(item=>item.valueRange);if(!tabs.length)fail(`GViz CSV did not expose any monthly HR tab after Sheets API failure: ${String(apiError?.message||apiError).slice(0,300)}`);return {accessMode:'public_gviz_csv_cache_busted',fallbackReason:String(apiError?.message||apiError||'').slice(0,500),clientEmail:serviceAccountInfo().clientEmail,title:'HR Guatemala - Sincronizacion Google Sheets',tabs,valueRanges,cacheBustApplied:true};}
async function readLiveHr(){try{return await readFromSheetsApi();}catch(error){console.warn('[HR live] Sheets API unavailable; using per-tab GViz CSV read-only fallback.');console.warn(String(error?.message||error).slice(0,800));return readFromGvizCsv(error);}}

function keyText(value){return normalizedText(value).replace(/[^a-z0-9ñ]+/g,' ').replace(/\s+/g,' ').trim();}
function findHeader(values){for(let index=0;index<Math.min(values.length,14);index++){const row=values[index]||[],cells=row.map(keyText);if(cells.includes('pais')&&cells.some(value=>value.includes('shopping')))return {index,row,cells};}return null;}
function resolveColumn(header,aliases){const needles=aliases.map(keyText).filter(Boolean);for(const needle of needles){const hits=header.cells.reduce((out,value,index)=>{if(value===needle)out.push(index);return out;},[]);if(hits.length===1)return hits[0];if(hits.length>1)return -2;}for(const needle of needles){const hits=header.cells.reduce((out,value,index)=>{if(value===needle||value.startsWith(`${needle} `)||value.endsWith(` ${needle}`))out.push(index);return out;},[]);if(hits.length===1)return hits[0];if(hits.length>1)return -2;}return -1;}
function cell(row,index){return index>=0?String(row?.[index]??'').trim():'';}
function isVisitRow(row,columns){return Boolean(cell(row,columns.pais)&&cell(row,columns.shopping)&&cell(row,columns.idCinema));}
function isShopperAssigned(value){const text=normalizedText(value);if(!text)return false;if(/^(p x asignar|por asignar|pendiente|sin asignar|no asignado|n\/a|na|ninguno|-|0|false)$/.test(text))return false;return !text.includes('p x asignar');}
function normalizeMoney(value){const text=String(value||'').trim();if(!text)return null;const normalized=text.replace(/[QL\s]/gi,'').replace(/\.(?=\d{3}(?:\D|$))/g,'').replace(/,/g,'.').replace(/[^0-9.-]/g,'');const number=Number(normalized);return Number.isFinite(number)?number:null;}
function serialDate(value){const number=Number(value);if(!Number.isFinite(number)||number<1||number>100000)return null;const date=new Date(Date.UTC(1899,11,30)+Math.floor(number)*86400000);return Number.isNaN(date.getTime())?null:date.toISOString().slice(0,10);}
function shortDate(day,month,period){let year=period.year;if(period.month===12&&month===1)year++;else if(period.month===1&&month===12)year--;const iso=`${year}-${pad(month)}-${pad(day)}`;return validIsoDate(iso)?iso:null;}
function normalizeDate(value,period){if(value==null||value==='')return null;const text=String(value).trim();if(!text)return null;if(/^\d{3,6}(?:\.\d+)?$/.test(text))return serialDate(text)||text;let match=text.match(/^(20\d{2})[-/]([01]?\d)[-/]([0-3]?\d)(?:\s.*)?$/);if(match){const iso=`${match[1]}-${pad(match[2])}-${pad(match[3])}`;return validIsoDate(iso)?iso:text;}match=text.match(/^([0-3]?\d)[/.-]([01]?\d)[/.-](20\d{2})(?:\s.*)?$/);if(match){const iso=`${match[3]}-${pad(match[2])}-${pad(match[1])}`;return validIsoDate(iso)?iso:text;}match=text.match(/(?:^|\s)([0-3]?\d)[-/.]([01]?\d)$/);if(match)return shortDate(Number(match[1]),Number(match[2]),period)||text;return text;}
function loadColumnMap(){if(!fs.existsSync(COLUMN_MAP_FILE))fail(`Missing column map: ${COLUMN_MAP_FILE}`);const contract=JSON.parse(fs.readFileSync(COLUMN_MAP_FILE,'utf8'));if(contract.contractId!=='tya-hr-column-map-r20-v1'||contract.resolutionPolicy!=='exact_or_unique_anchored_only')fail('Column map identity/policy mismatch.');return contract;}

function parseTab(tab,values,columnMap){const header=findHeader(values||[]);if(!header)return {visits:[],issues:[{code:'header_not_found',severity:'critical',tab:tab.tabTitle}],diagnostic:{title:tab.tabTitle,country:tab.country,periodKey:tab.key,rows:0,headerRow:null,columns:{}}};const columns={},issues=[];for(const [name,spec] of Object.entries(columnMap.columns||{})){columns[name]=resolveColumn(header,spec.aliases||[]);if(columns[name]===-2)issues.push({code:'column_ambiguous',severity:spec.critical?'critical':'warning',tab:tab.tabTitle,column:name});else if(columns[name]<0&&spec.critical)issues.push({code:'column_missing',severity:'critical',tab:tab.tabTitle,column:name});else if(columns[name]<0)issues.push({code:'column_missing_optional',severity:'warning',tab:tab.tabTitle,column:name});}if(issues.some(issue=>issue.severity==='critical'))return {visits:[],issues,diagnostic:{title:tab.tabTitle,country:tab.country,periodKey:tab.key,rows:0,headerRow:header.index+1,columns}};
  const visits=[],shopperSignals={assigned:0,unassigned:0,placeholder:0},columnDiagnostics={};for(const [name,index] of Object.entries(columns))columnDiagnostics[name]={index,header:index>=0?cell(header.row,index):null,nonEmptyVisitRows:0};
  for(let rowIndex=header.index+1;rowIndex<values.length;rowIndex++){const row=values[rowIndex]||[];if(!isVisitRow(row,columns))continue;for(const [name,index] of Object.entries(columns))if(index>=0&&cell(row,index))columnDiagnostics[name].nonEmptyVisitRows++;const shopperRaw=cell(row,columns.shopper),hasShopper=isShopperAssigned(shopperRaw);if(hasShopper)shopperSignals.assigned++;else{shopperSignals.unassigned++;if(shopperRaw)shopperSignals.placeholder++;}const cinemaId=cell(row,columns.idCinema),shopping=cell(row,columns.shopping),visitId=`hr_${tab.key}_${tab.country.toLowerCase()}_${rowIndex+1}_${safeHash([cinemaId,shopping,cell(row,columns.quincena),cell(row,columns.franja),rowIndex+1].join('|'),'v').slice(-10)}`,shopperId=hasShopper?safeHash(shopperRaw,`shopper_${tab.country.toLowerCase()}`):null;visits.push(applyCanonicalVisitState({id:visitId,hrRowId:`${tab.tabTitle}!${rowIndex+1}`,sourceTab:tab.tabTitle,sourceRow:rowIndex+1,tenantId:'tya',projectId:'cinepolis',program:'cinepolis',periodKey:tab.key,periodLabel:tab.label,pais:tab.country,country:tab.country,cinemaId,sucursal:shopping,ciudad:cell(row,columns.ciudad),quincena:cell(row,columns.quincena),franja:cell(row,columns.franja),franjaCode:normalizedText(cell(row,columns.franja)).includes('wknd')?'WKND':'WK',formato:cell(row,columns.formato),escenario:cell(row,columns.tipoCompra),tipoCombo:cell(row,columns.tipoCombo),metodoPago:cell(row,columns.metodoPago),disponibleDesde:normalizeDate(cell(row,columns.disponibleDesde),tab),agendada:normalizeDate(cell(row,columns.fechaProgramada),tab),realizada:normalizeDate(cell(row,columns.fechaRealizada),tab),cuestFecha:normalizeDate(cell(row,columns.fechaCuestionario),tab),submittedAt:normalizeDate(cell(row,columns.fechaSubmitido),tab),controlDia:cell(row,columns.controlDia),shopperId,shopper:hasShopper?'Shopper protegido':null,shopperCode:hasShopper?safeHash(shopperRaw,`TYA_${tab.country}`).toUpperCase():null,hasShopper,currency:tab.country==='HN'?'L':'Q',honorario:normalizeMoney(cell(row,columns.honorarios)),boleto:normalizeMoney(cell(row,columns.precioBoleto)),comboAmt:normalizeMoney(cell(row,columns.precioCombo)),sourceSafe:true,piiProtected:true}));}
  return {visits,issues,diagnostic:{title:tab.tabTitle,country:tab.country,periodKey:tab.key,rows:visits.length,headerRow:header.index+1,shopperSignals,columnDiagnostics}};
}
function buildPeriods(visits,tabs){const map=new Map();for(const tab of tabs)if(!map.has(tab.key))map.set(tab.key,{key:tab.key,id:`cinepolis-${tab.key}`,projectId:'cinepolis',label:tab.label,fullLabel:tab.fullLabel,year:tab.year,month:tab.month,visits:0,byCountry:{GT:0,HN:0}});for(const visit of visits){const row=map.get(visit.periodKey);if(row){row.visits++;row.byCountry[visit.country]=(row.byCountry[visit.country]||0)+1;}}return [...map.values()].sort((a,b)=>a.key.localeCompare(b.key));}
function buildShoppers(visits){const map=new Map();for(const visit of visits){if(!visit.canonicalFacets?.assigned||!visit.shopperId)continue;const current=map.get(visit.shopperId)||{id:visit.shopperId,shopperId:visit.shopperId,code:visit.shopperCode,nombre:'Shopper protegido',pais:visit.country,ciudad:visit.ciudad||'',estado:null,status:null,rating:null,completion:null,preference:null,honorario:null,perfilCompleto:false,dataLevel:'protected_reference',operationalProfileAvailable:false,fullAuthorizedProfileAvailable:false,visitas:0,realizadas:0,submitidas:0,liquidationCandidates:0,liquidadas:0,pagadas:0,sourceSafe:true,piiProtected:true};current.visitas++;current.realizadas+=visit.canonicalFacets.realized?1:0;current.submitidas+=visit.canonicalFacets.submitted?1:0;current.liquidationCandidates+=visit.canonicalFacets.liquidationCandidate?1:0;current.liquidadas+=visit.canonicalFacets.liquidationConfirmed?1:0;current.pagadas+=visit.canonicalFacets.paymentConfirmed?1:0;map.set(visit.shopperId,current);}return [...map.values()].sort((a,b)=>String(a.code||'').localeCompare(String(b.code||'')));}

async function main(){const columnMap=loadColumnMap(),live=await readLiveHr(),visits=[],issues=[],diagnostics=[];for(let index=0;index<live.tabs.length;index++){const parsed=parseTab(live.tabs[index],live.valueRanges[index]?.values||[],columnMap);visits.push(...parsed.visits);issues.push(...parsed.issues);diagnostics.push(parsed.diagnostic);}const blockers=issues.filter(issue=>issue.severity==='critical');if(blockers.length)fail(`R20 HR mapping HOLD: ${JSON.stringify(blockers).slice(0,5000)}`);const periods=buildPeriods(visits,live.tabs),history=validateCanonicalHistory(visits,periods);if(history.decision!=='PASS_CANONICAL_HISTORY')fail(`R20 canonical history HOLD: ${JSON.stringify(history.issues).slice(0,3000)}`);const shoppers=buildShoppers(visits),summaries=summarizeCanonicalPeriods(visits),byStatus={},byCountry={};for(const visit of visits){byStatus[visit.estado]=(byStatus[visit.estado]||0)+1;byCountry[visit.country]=(byCountry[visit.country]||0)+1;}const counts={periods:periods.length,tabs:live.tabs.length,visits:visits.length,shoppers:shoppers.length,byStatus,byCountry,assigned:visits.filter(v=>v.canonicalFacets.assigned).length,unassigned:visits.filter(v=>!v.canonicalFacets.assigned).length,scheduled:visits.filter(v=>v.canonicalFacets.scheduled).length,realized:visits.filter(v=>v.canonicalFacets.realized).length,questionnaireCompleted:visits.filter(v=>v.canonicalFacets.questionnaire).length,submitted:visits.filter(v=>v.canonicalFacets.submitted).length,liquidationCandidatesPendingFinancialMatch:visits.filter(v=>v.liquidationState==='candidate_pending_financial_match').length,liquidationConfirmed:visits.filter(v=>v.liquidationState==='confirmed').length,paymentConfirmed:visits.filter(v=>v.paymentState==='confirmed').length,reviewRequired:visits.filter(v=>v.reviewRequired===true).length};const info=serviceAccountInfo();const snapshot={generatedAt:GENERATED_AT,buildLabel:process.env.CXORBIA_DEV_BUILD_LABEL||'tya-live-hr-source-safe-r20',tenantId:'tya',tenantName:'TyA',projectId:'cinepolis',projectName:'Cinépolis',source:{type:'google_sheets_live_multitab',accessMode:live.accessMode,fallbackReason:live.fallbackReason||null,title:live.title,spreadsheetIdMasked:`${SHEET_ID.slice(0,6)}...${SHEET_ID.slice(-4)}`,serviceAccountProject:info.projectId||'cxorbia-backend-dev',serviceAccountEmailMasked:(live.clientEmail||'').replace(/^[^@]+/,'***'),cacheBustApplied:live.cacheBustApplied===true,sourceSafe:true,piiExcluded:['nombre_shopper','telefono','mail','dpi','banco','direccion_shopper','observaciones','hr_url_privada','workbook_crudo'],semanticNormalizer:'r15g+r20',columnMapContract:columnMap.contractId,columnResolver:columnMap.resolutionPolicy,mappingFailClosed:true,dateFormat:'YYYY-MM-DD',submissionLiquidationSeparated:true,canonicalStateAcrossAllDetectedPeriods:true,buildTimeSnapshot:true,runtimeLiveSync:false},tenantConfig:{tenantId:'tya',tenantName:'TyA',configurable:true},projectConfig:{projectId:'cinepolis',projectName:'Cinépolis',configurable:true,countries:['GT','HN'],questionnaireSource:'TyAOnline',hrSourceType:'google_sheets_live_multitab'},sourceSafe:true,imported:false,production:false,periods,visits,shoppers,counts,periodOperationalSummary:summaries,tabsRead:diagnostics,issues,normalization:{version:'R20-live',periodCount:periods.length,periodKeys:periods.map(row=>row.key),historyScope:'all_detected_hr_periods',cacheBustApplied:live.cacheBustApplied===true,rules:['sheets_api_or_per_tab_gviz_live_read','contract_driven_column_map','strict_unique_header_resolution','empty_or_placeholder_shopper_is_unassigned','valid_dates_by_period_context','submitted_is_not_liquidated_or_paid','financial_confirmation_requires_external_source','conflicts_require_review']},safeState:{writes:false,imports:false,deploy:false,production:false,providers:false,payments:false}};fs.mkdirSync(path.dirname(OUT_FILE),{recursive:true});fs.writeFileSync(OUT_FILE,['/* CXOrbia TyA live HR source-safe R20 payload; no PII/raw workbook. */','window.CX_TYA_HR_SOURCE_SAFE = ',JSON.stringify(snapshot,null,2),';'].join(''),'utf8');fs.mkdirSync(GATE_OUT,{recursive:true});fs.writeFileSync(path.join(GATE_OUT,'hr-header-column-diagnostics.source-safe.json'),JSON.stringify({schemaVersion:'3.0.0',sourceSafe:true,containsDataRows:false,containsPii:false,generatedAt:GENERATED_AT,accessMode:live.accessMode,cacheBustApplied:live.cacheBustApplied===true,historyScope:'all_detected_hr_periods',columnMapContract:columnMap.contractId,tabs:diagnostics,issues,blockers},null,2)+'\n','utf8');console.log(JSON.stringify({decision:'PASS_R20_LIVE_CANONICAL_HR_HISTORY',outFile:OUT_FILE,accessMode:live.accessMode,cacheBustApplied:live.cacheBustApplied===true,counts,periods:periods.map(row=>row.key),safeState:snapshot.safeState},null,2));}

main().catch(error=>{console.error(`R20_LIVE_HR_BUILD_HOLD: ${String(error?.message||error)}`);process.exit(1);});
